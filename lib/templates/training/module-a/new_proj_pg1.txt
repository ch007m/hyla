:data-uri:
:icons: font
:last-update-label!:

:numbered!:
== Unordered bullet List without numbers
* BPMS6 Provides a much improved development desktop called the `KIE Drools Workbeanch`
* KIE = Knowledge is Everything

:numbered!:
== Unordered bullet List without numbers (including title)
.Title of a unordered list
* BPMS6 Provides a much improved development desktop called the `KIE Drools Workbeanch`
* KIE = Knowledge is Everything 2

:numbered:
== Numbered
=== Numbered con't
. Select *Knowledge Bases* on the left hand pallet.
. Select *Create New → New BPMN2 Process*.  You will get a dialog box.
. Name the new process *policyquoteprocess*
. For the package, *select org.acme.insurance.pricing*
. Click *Ok*

:numbered!:
== Nested unordered list
* West wood maze
** Maze heart
*** Reflection pool

== Image
.Text of the picture
image::images/lab-brms-1.png[width="45%"]

== Admonition

NOTE: This is an example note 1.

TIP: The use of Strings instead of Integers is intentional.  The variables will be used in a later lab as fields in an HTML form that passes values as HTTP GET parameters.  These are always Strings.  This means that means that any process variable set directly (age, numberOfAccidents, numberOfTickets, vehicleYear) or indirectly (price, as it's derived from taskPrice) through the forms must be declared as String.

WARNING: DO NOT use the generate task form templates button.  If used, it will overwrite all the form in your process instead of just the new form.

== Table

.An example table
[width="60%",options="header"]
|==============================================
| Option          | Description
| -a 'USER GROUP' | Add 'USER' to 'GROUP'.
| -R 'GROUP'      | Disables access to 'GROUP'.
|==============================================

ifdef::showScript[]

endif::showScript[]

